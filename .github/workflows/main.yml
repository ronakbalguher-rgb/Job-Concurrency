name: Build Next.JS APP

on: 
  push:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest

    concurrency:
      group: Deploy
      cancel-in-progress: true

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v7.0.0
      - name: Install Dependencies
        working-directory: ./my-workflow-app
        run: npm install
      - name: Build Next.JS
        working-directory: ./my-workflow-app
        run: npm run build
      - name: Deploy App
        run: echo "Deploy App...."
      
